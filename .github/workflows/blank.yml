name: Change Title on issue

on:
  issues:
    types: [opened, edited, milestoned]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      
    - name: Run a multi-line script
      run: "'${{ github.event.issue.title }}' -replace '.*to: '"
      shell: pwsh

